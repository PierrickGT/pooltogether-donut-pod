{
  "name": "@pooltogether-pod/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "pooltogether",
    "workspaces",
    "yarn"
  ],
  "license": "MIT",
  "private": true,
  "scripts": {
    "react-app:build": "yarn workspace react-app build --max-old-space-size=12288",
    "react-app:eject": "yarn workspace react-app eject",
    "react-app:test": "yarn workspace react-app test",
    "ship": "echo 'Sorry, this does not work yet'",
    "format": "yarn workspace react-app format",
    "format:file": "yarn workspace react-app format:file",
    "lint": "yarn workspace react-app lint",
    "lint:fix": "yarn workspace react-app lint:fix",
    "lint:file": "yarn workspace react-app lint:file",
    "quality": "yarn workspace react-app quality",
    "build": "yarn workspace react-app build --max-old-space-size=12288",
    "chain": "yarn workspace buidler chain",
    "test": "yarn workspace buidler test",
    "start": "yarn workspace react-app start",
    "compile": "yarn workspace buidler compile",
    "deploy": "yarn workspace buidler run scripts/deploy.js && npx buidler run scripts/publish.js",
    "watch": "cd packages/buidler && node scripts/watch.js",
    "accounts": "yarn workspace buidler accounts",
    "balance": "yarn workspace buidler balance",
    "send": "yarn workspace buidler send"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
